"use client";

// Phase 3: full implementation
// Placeholder — renders assets as a responsive table (desktop) / cards (mobile)
export function AssetTable() {
  return <div className="text-muted-foreground">Asset table — Phase 3</div>;
}
